<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
	<modelVersion>4.0.0</modelVersion>
	<parent>
		<groupId>org.springframework.boot</groupId>
		<artifactId>spring-boot-starter-parent</artifactId>
		<version>3.5.10</version>
		<relativePath/> <!-- lookup parent from repository -->
	</parent>
	<groupId>com.fiscore</groupId>
	<artifactId>core</artifactId>
	<version>V0.0.1-alpha</version>
	<packaging>war</packaging>
	<name>fiscore</name>
	<description>Fiscore es una plataforma SaaS para la gestión integral de facturación, suscripciones y control operativo de despachos contables.</description>
	<url/>
	<licenses>
		<license/>
	</licenses>
	<developers>
		<developer/>
	</developers>
	<scm>
		<connection/>
		<developerConnection/>
		<tag>V0.0.1-alpha</tag>
		<url/>
	</scm>
	<properties>
		<java.version>17</java.version>
		<build.number>1.0.1</build.number>
		<user.timezone>America/El_Salvador</user.timezone>
		<maven.build.timestamp.format>yyyyMMddHHmmss</maven.build.timestamp.format>
	</properties>
	<dependencies>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-web</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-security</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-crypto</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-thymeleaf</artifactId>
		</dependency>
		<dependency>
			<groupId>org.thymeleaf</groupId>
			<artifactId>thymeleaf</artifactId>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-tomcat</artifactId>
			<scope>provided</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-test</artifactId>
			<scope>test</scope>
		</dependency>
		<dependency>
			<groupId>org.springframework.boot</groupId>
			<artifactId>spring-boot-starter-data-jpa</artifactId>
		</dependency>
		<dependency>
			<groupId>org.postgresql</groupId>
			<artifactId>postgresql</artifactId>
			<scope>runtime</scope>
		</dependency>
		<dependency>
			<groupId>org.projectlombok</groupId>
			<artifactId>lombok</artifactId>
			<scope>provided</scope>
		</dependency>
		<!-- Base de datos en memoria para las pruebas de integración -->
		<dependency>
			<groupId>com.h2database</groupId>
			<artifactId>h2</artifactId>
			<scope>test</scope>
		</dependency>
		<!-- Utilidades de test para autenticación y CSRF -->
		<dependency>
			<groupId>org.springframework.security</groupId>
			<artifactId>spring-security-test</artifactId>
			<scope>test</scope>
		</dependency>
	</dependencies>

	<build>
		<plugins>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-war-plugin</artifactId>
				<configuration>
					<!--
					  Salvaguarda: WEB-INF/lib lo llena Maven con las dependencias, no debe
					  usarse para dejar jar a mano. Uno con espacios en el nombre impide
					  arrancar el WAR ejecutable, porque Tomcat escanea ese directorio, la
					  ruta se codifica como %20 y la entrada no resuelve
					  (NullPointerException en JarWarResourceSet).
					  Solo filtra lo copiado desde src/main/webapp; las dependencias de
					  Maven no se ven afectadas.
					-->
					<warSourceExcludes>WEB-INF/lib/**</warSourceExcludes>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.apache.maven.plugins</groupId>
				<artifactId>maven-antrun-plugin</artifactId>
				<version>1.8</version>
				<executions>
					<execution>
						<phase>validate</phase>
						<goals>
							<goal>run</goal>
						</goals>
						<configuration>
							<target>
								<property name="user.timezone" value="America/El_Salvador"/>
								<property name="build.number" value="${maven.build.timestamp}" />
								<property name="timestamp" value="${maven.build.timestamp}" />
							</target>
						</configuration>
					</execution>
				</executions>
				<configuration>
					<exportAntProperties>true</exportAntProperties>
				</configuration>
			</plugin>
			<plugin>
				<groupId>org.springframework.boot</groupId>
				<artifactId>spring-boot-maven-plugin</artifactId>
				<executions>
					<execution>
						<goals>
							<goal>repackage</goal>
							<goal>build-info</goal>
						</goals>
					</execution>
				</executions>
				<configuration>
					<additionalProperties>
						<timeZone>America/El_Salvador</timeZone>
					</additionalProperties>
					<!--suppress UnresolvedMavenProperty -->
					<finalName>${project.artifactId}-${project.version}-${timestamp}</finalName>
				</configuration>
			</plugin>
		</plugins>
	</build>

</project>
